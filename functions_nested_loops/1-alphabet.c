#include "main.h"

/**
 * main - prints the alphabet
 *
 * Return: always 0
 */

void print_alphabet(void);
{
	char alphabet;
	{
		for (alphabet in range(97, 122):
		putchar(chr(alphabet));
	}	
	return (0);
}

#include "main.h"

/**
 * prints_alphabet -  prints the alphabet.
 *
 */

void prints_alphabet(void)
{
	int letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

	_putchar('\n');
}

#include "main.h"

/**
 * prints_alphabet - entry point
 *
 * Return: always 0
 */

void print_alphabet(void);
{	
	char alphabet;

		for (alphabet = 'a'; alphabet = 'z'):
		putchar(alphabet);
		putchar('\n')
	return (0);
}

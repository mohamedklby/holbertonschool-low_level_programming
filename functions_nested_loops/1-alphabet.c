#include "main.h"

/**
 * prints_alphabet - entry point
 *
 * Return: always 0
 */

void prints_alphabet(void);

{
	char alphabet;

	for (alphabet = 'a'; alphabet = 'z')
	{

		putchar(alphabet);

	}
		putchar('\n');
}

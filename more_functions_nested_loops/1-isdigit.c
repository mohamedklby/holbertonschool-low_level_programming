#include "main.h"

/**
 * _isdigit - check if a caracter is uppercase
 * @c: character to check
 *
 * Return: 1 if uppercase, 0 otherwise
 */

int _isdigit(int c)

{
	if (c >= '0' && c <= '9')
		return (1);

			return (0);
}

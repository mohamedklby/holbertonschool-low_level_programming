#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies deux entiers
 * @a: le premier entier
 * @b: le deuxi√me entier
 *
 * Return: always 0 (Success)
 */

int mul(int a, int b)
{
	a = 784 * 128;
	printf("Cas 1 : %d\n", a);

	b = -1024 * 1608;
	printf("Cas 2 : %d\n", b);

	return (0);
}

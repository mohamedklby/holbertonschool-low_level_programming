#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies deux entiers
 * @a: le premier entier
 * @b: le deuxiÃme entier
 *
 * Return: always 0 (SuccessÂ°
 */

int mul(int a, int b)
{

	printf("%d\n", 784 * 128);
	printf("%d\n", -1024 * 1608);

		return (a && b);

	return (0);
}

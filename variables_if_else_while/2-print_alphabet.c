#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Return 0 (if success)
 */

int main()
{
	char alpha;

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
	putchar(alpha);
	}
	putchar('\n');
	return (0);
}

#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * Return 0 (if success)
 */

int main()
{
	char lettre;

	for (lettre = 'a' ; lettre <= 'z' ; lettre++)
	{
		putchar(lettre);
	putchar("\n");
	return (0);
}

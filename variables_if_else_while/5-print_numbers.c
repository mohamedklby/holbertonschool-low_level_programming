#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
 */

int main(void)
{
	int nombre;

	for (nombre = 0; nombre < 10; nombre++)

	printf("%d", nombre);
	printf("\n");

	return (0);
}

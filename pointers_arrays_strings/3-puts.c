#include "main.h"
#include <stdio.h>

/**
 * _puts - function that prints a string, followed by a new line.
 * @str : string to print
 * Return: i
 */

void _puts(char *str)
{
	int vip;

	l = 0;
	while (vip[l] != '\0')
		_putchar(vip[l]);
	l++;
	putchar ('\n');
}
